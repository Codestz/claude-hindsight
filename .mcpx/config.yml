# Generated by mcpx init from .mcp.json
# See: mcpx --help

servers:
    serena:
        command: serena
        args:
            - start-mcp-server
            - --context
            - claude-code
            - --project
            - /Users/codestz/Documents/PersonalProjects/claude-hindsight
        transport: stdio
        daemon: true
        startup_timeout: 30s
